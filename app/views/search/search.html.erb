<div class="container">
	<div class="row">
		<div class="col-xs-3">
            <%= render 'users/info', user: current_user %>
        </div>
        <div class="col-xs-9">
        	<% if @model == 'user'  %>
        	<%= render 'users/index',users: @datas %>
        	<% else %>
        	<%= render 'books/index',books: @datas %>
        	<% end %>
        </div>